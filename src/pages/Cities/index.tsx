import { FlexContainer } from "../../components/styled/FlexContainer";

const Cities = () => {
  return (
    <FlexContainer className="cities__container">
      Cities
    </FlexContainer>
  )
}

export default Cities;